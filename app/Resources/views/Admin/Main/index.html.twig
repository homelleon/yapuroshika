{# AppBundle:Security:Admin:index.html.twig
============================================
-----Template to show admin main page------#}

{% extends ':Admin:admin_base.html.twig' %}

{% block content %}    
    Админ-панель
    <div class="admin__navigation">
        <a class="admin__row button button_orange" 
           href="{{ path('main') }}">
            Вернуться на сайт
        </a>
        <a class="admin__row button button_service" 
           href="{{ path('admin_articles') }}">
            Администрирование статей
        </a>
        <a class="admin__row button button_service" 
           href="{{ path('admin_users') }}">
            Администрирование пользователей
        </a>
        <a class="admin__row button button_service"
           href="{{ path('roles') }}">
            Администрирование ролей
        </a>
    </div>    
{% endblock %}

{% block admin_side %}
    Index side
{% endblock %}